1. commit = change.
2. making changes in github is a two step process, first make a ADD and then commit.
3. ~ represnts the root position of our gitbash.
4. To configure in GitBash.

write 1. git config --global user.name "github_account_name"
2. git config --global user.email "email_used_in_github_account"
3. to see what we have saved the commond is git config --list

4. To Clone a repo. from the github to our Local memory( i.e. Laptop or PC) we use a clone command --> "git clone <-somelink->"
5. To enter the file or want to change the directory then we use a command i.e. "cd <-Name of directory->"
5.1. To comout form a repo. we use the command "cd .."
6. To check teh number of files we can use this command "ls" means list files.
7. To all repos. even those whicha re hidden then we use command " ls -a". means all repos.
8. To check the status of the repos. we use the command "git status"
9. When we made a change in our file either in github repo. or directly on Vscode we always need to save it.
10. To save a file or change in github we need to make 2 step process i.e. first ADD nad then COMMIT.
11. To ADD file we writethe command i.e. "git add <-file_name->". for bulk files we use "git add ."
12. To commit the change to the github we write the command i.e. "git commit -m "some_massage""
13. To show these commits and changes to the github from local memory we use the command "git push origin main"
14. To make a new folder or repo in out Code editor system directly we can write the command "makedir <-Folder_name->".
15. To connect the repo. or new folder with Git we use to make folder gitrepo first.
16. To make folder a git repo. we first initiallise it with the command "git init"
17. when we initialise a git repo. then we need to add origin first to initiallise origin.
18. To initiallise origin use command, git remote add origin <-link->.
19. To verify remote use command, git remote -v.
20. To check branch use command, git branch
21. To rename a branch we use command, git branch -m new_name
22. To make a new branch we use the command, git checkout -b new_branch_name
23. To change branch we use the command, git checkout branchname_where_to go
24. To delete a branch we use hte command, git branch -d branch_name
25. 